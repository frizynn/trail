# Build output
dist/
*.tsbuildinfo

# Dependencies
node_modules/

# Bun
bun.lockb

# OS
.DS_Store

# Obsidian per-user workspace state (the vault config itself IS committed; this is just local UI state)
.trail/.obsidian/workspace.json
.trail/.obsidian/workspace-mobile.json

# Trail runtime locks (never commit active claims)
.trail/.locks/
